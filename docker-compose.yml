services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # ensure the app binds the port the container exposes
      PORT: 3000
    restart: unless-stopped
